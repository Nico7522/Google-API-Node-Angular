import { ToFrenchDatePipe } from './to-french-date-pipe';

describe('ToFrenchDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ToFrenchDatePipe();
    expect(pipe).toBeTruthy();
  });
});
